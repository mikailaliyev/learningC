#include <stdio.h>

int main() {
  //   const int NUM = 5;
  //   printf("%d\n", NUM);
  //   NUM = 0; //Will through an error
  //   printf("%d", NUM);

  // String also constant or immutable
  printf("Hello\n");

  // Numbers printed withour assigned to variables are
  // also constants, so we have to manually edit them
  printf("%d", 45);
  return 0;
}