#include <stdio.h>

int main() {
    // int index = 0;
    // while(index <= 10) {
    //     printf("The index is %i\n", index);
    //     index++;
    // }

    int index = 6;
    do
    {
      printf("The index is %i\n", index);
      index++;
    } while (index <= 5);
    return 0;
}